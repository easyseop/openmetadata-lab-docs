# 작업 히스토리 — 2026-08-10 (다른 컴퓨터 이어받기용 핸드오프)

> ⚠️ 로컬 메모리(`~/.claude`)는 다른 컴퓨터엔 없다. 이 문서에 오늘 맥락을 전부 담는다.
> 이어받는 컴퓨터는 이 파일부터 읽는다.

---

## 0. 환경 사실 (재현·접속용)

| 항목 | 값 |
|---|---|
| OM 1.13.2 서버 | `docker compose -f docker/development/docker-compose.yml -f docker/development/override-1.13.2.yml up -d openmetadata-server` (@ `~/om-work/om-temp-real-1.13.1`) |
| 접속 | http://127.0.0.1:8585 · admin@open-metadata.org / admin (env `OM_ADMIN_PASSWORD`) · rev 9587fe8f, 1.13.1 데이터 마이그레이션본 |
| 검사기(하네스) | `/Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/work/review-openmetadata-test-phase-bundling/harness` (`acgh` 패키지) |
| BANK-OM 등록 | `harness/registrations/kb-openmetadata/` — manifests/BANK-OM-001~011.yaml, customization-registry.yaml, contracts.yaml, policies(sensitive-zones·repository-layout·debt-thresholds) 등 (관리 파일 ~25개) |
| 계약 테스트 | `.../review-openmetadata-test-phase-bundling/tests/bank/contracts/` |
| OM 소스 빌드 | `~/om-work/om-1.13.2-build` |
| webtest-agent(화면검증 봇) | github.com/easyseop/test-agent, 브랜치 `claude/web-app-test-agent-yyc2eq`, 로컬 `~/om-work/test-agent` (`.venv` python3.11) |

## 1. 오늘 한 일 (3 스트림)

### A. 검사기 능력·한계 정직 분석 → CODEX-05 정본
- 산출: `docs/CODEX-05-검사기-한계-정직분석-및-LLM검증.md` (Codex 검토 반영된 **정본**).
- 방법: read-only 에이전트 4개로 하네스 전수 정독(게이트 엔진 / T-게이트·매니페스트 / 계약·테스트 / 전략문서).
- 핵심 한계: ①리로케이션(파일 이동) 못 잡음 ②“공식과 다름 ≠ 기능 온전” ③승인자·충돌률 등 사람 입력값 미검증 ④관리 파일 과다(지속운영 부담).
- Codex 정정(재사용 금지 표현): “Runtime 거의 0회”(→1.13.1 필수 Contract 9/9 통과), “위조 불가”(→변경 ‘감지’), “patch-kill만 행동검증”(→Runtime도 행동), 현재구현 vs 향후설계 구분, **1.13.2 = 부분 검증(최종 PASS 아님)**.

### B. webtest-agent 2차 재테스트 (화면 검증)
- 설치: 3.11 venv + `pip install -e ".[db]"` + `playwright install chromium`. 데모 21/21, `pytest` 441 통과, `check_ime.py`(fill 통과·type_ime 실패=정상 → **한글 IME 검증 실동작 확인**).
- 픽스처(실제 존재): 확장컬럼 = `bank_contract_runtime.bank_contract_db.bank_contract_schema.bank_contract_table`(컬럼 `bank_contract_column`, ext=contractAttribute/contractInstance/contractInfo); 실패 assertion = 각 DB `...account_transaction.transaction_id.mock_transaction_id_not_null_fail`(owner admin).
- 설정 `configs/openmetadata.yaml`: `auth.per_context:true`(OM은 storage_state로 세션 복원 안 됨→매 시나리오 재로그인), `target.locale:en-US`(화면을 정답원 영어와 맞춰 **value_map 불필요** + i18next 경고 소거), ignore_http는 loggedInUser·ingestion(airflow 미기동)·dataContracts만.
- **결과(마스킹 없음)**: 화면 A(확장컬럼) `matched`, 화면 B(품질목록) `matched` — 데이터 실제 일치. **유일 red = `/images/governance.png`·`data-collaboration.png` 404**(로그인 화면 장식 이미지 = 별개 경미 하자, OM 빌드 확인 대상; 확장/품질 기능과 무관).
- 미작성 케이스(다음): 검색·Explore트리·버전·401/403(fetch expect_status)·한글IME(SchemaEditor 경로+CodeMirror 셀렉터 필요). CRUD(쓰기)는 제외. 범위 밖(빌드·서버로그·로케일JSON·재기동·이미지)=셸/CI.
- 산출: 회신 `~/Downloads/webtest-openmetadata-회신.md`, 결과 `~/om-work/test-agent/결과.zip`, 최신 리포트 `~/om-work/test-agent/runs/20260810-142331/`.
- 도구 관련 정정(중요): `ignore_console_patterns`·`value_map`은 **쓰지 않기로**(은폐 위험; i18next missing-key는 실제 결함 신호일 수 있음). en-US 로케일로 근본 해결.

### C. 전략 발표덱 (리더 보고용)
- 산출: `docs/CODEX-05-전략발표.html`(Artifact), `docs/CODEX-05-전략발표.pptx`(편집용), `docs/CODEX-05-전략발표_gen.py`(python-pptx 생성기 — 다른 컴퓨터에서 `pip install python-pptx` 후 실행하면 pptx 재생성).
- **Artifact URL(비공개)**: https://claude.ai/code/artifact/2d426d5c-9bca-4bc9-b8b8-a3076541634d — 갱신은 같은 파일 재발행(같은 세션) 또는 다른 세션에선 `url` 인자로 이 주소 지정.

## 2. 발표덱 서사 (15장) — 편집 시 유지할 논지
1. 표지 — 전제: 행내 운영 서비스라 보수적으로 → AI 개입 최소화 전략에서 출발
2. 배경·전제
3. 규칙기반 검사기(파일위치·커밋·안전·못박기) + **BANK-OM-001 실제 예시**(manifest + contract) + **관리 파일 ~25개**
4. **업그레이드 테스트**: 1.13.0→1.13.1 성공 / 1.13.1→1.13.2 실패(병합 충돌·리로케이션·관리파일/커밋이력 운영부담) → 결론: 작은 변화 OK, 구조 변화 크면 오판
5. 강점 = **왜 룰베이스인가**(LLM 못 하는 것): patch-kill·fail-closed·못박기·기계대조·결정론 (각 항목에 “LLM이라면 X → 검사기라서 Y”)
6. 한계① — **지속 가능 운영**(관리 파일 과다)
7. 한계② — 리로케이션/의미(반쪽병합)/입력(사람값) **실제 사례**
8. 8/5 공유 ‘LLM 기반 운영 방식’ 검토 (까기 아님, 보완 제안)
9. 방향 — LLM이 일하고 검사기가 근거 뒷받침(하이브리드)
10. **도입 제안** — BANK-OM 라이프사이클 6단계(등록→범위검사→ID별 코드검사→전용 Contract→못박기→승인·증거) + “파일 관리 부담은 LLM이, 검사기는 검증만”
11. **7개 기능별 Contract 표**(BANK-OM-001~007: API/화면/소스·중요도·상태)
12. **정직한 검증 현황**(1.13.1 9/9 · patch-kill 부분(Sybase·Tibero만) · 1.13.2 부분 · 대조기 향후설계)
13. 왜 MD 아니라 검사기 — 대표 예시 4개(전부가 아님, 실제 도움=6단계×7계약)
14. 독립성 조건 + 배포 4요소
15. 결론 — 작은변화 OK/큰변화 한계 → 예측가능 범위엔 룰베이스, 불확실엔 하이브리드 / **통제 두 축**(①방식 ②결과를 예측가능 형태로 강제) / “AI를 minimize가 아니라 harness(고삐)”

### 필수 정확도(발표 문구)
- “필수 Contract 9개” = **API 4 · 브라우저(화면) 3 · 소스 정합성 2**(= BANK-OM-001~007 required_tests 합). 9개 전부가 서버 동작 검사는 아님.
- 무결성 digest = 변경 ‘감지’(전자서명·외부공증 아님, 보호된 CI에서 생성·보관 시 임의변경 감지).
- 리로케이션 오판 = 코드가 다른 파일로 이사(예: Sybase `case DatabaseServiceType.Sybase`가 `DatabaseServiceUtils.tsx`→`*PureUtils.ts`)했는데 옛 경로만 봐서 “사라졌다”고 BLOCK.

## 3. 대기·다음 (미반영, 사용자 “일단 둬봐”)
- 결론 갭 3가지: ①배포 4요소가 S15에 미포함 ②S4 “FAIL” 뉘앙스(제품 결함 아님 주석) ③S5(vs LLM)·S13(vs MD) 중복감.
- 4페이지 어려움 목록 A안(리로케이션·병합 충돌·생성코드 소실·마이그레이션 체크섬 누락·픽스처·환경) — 넣을지 대기.
- **pptx를 HTML 최신 서사로 동기화 필요**(현재 pptx는 라이프사이클·표까진 있으나 4·5·결론 세부는 HTML이 더 최신). → `docs/CODEX-05-전략발표_gen.py` 수정 후 재생성.
- webtest 남은 케이스 작성(검색/트리/버전/401·403/IME) + sybase·tibero 계약 재-anchor(`*PureUtils.ts`).

## 4. 메모리 스냅샷 (다른 컴퓨터엔 없으니 여기 보존)
- **사용자 계정**: GitHub `easyseop` (개인 이메일은 공개 저장소라 생략).
- **보고 스타일 선호**: 기술용어 유지 + 각 핵심 뒤 **“즉, ~~~입니다”** 평이한 의역 병기(비개발자도 상황 파악 쉽게). ※ 스킬로 만들어둠: `~/.claude/skills/plain-report/SKILL.md` — **다른 컴퓨터엔 없으니 필요시 재생성**.
- **openmetadata-lab**: OM 1.13.1 커스텀 버전업 검증 랩(Colima 기반, `~/openmetadata-lab`).
- **프로젝트 목표**: 은행 커스터마이징(BANK-OM-001~007 = InstanceCode·QueryReport·DataAssertions·확장컬럼·한글IME·Sybase·Tibero)이 공식 버전업 후에도 생존·동작하는지 검사기(하네스)로 검증.
- **전략 결론(정본)**: LLM 제안 → 정적 검사기(범위·정합성) → 독립 CI Runtime Contract → 권한자 승인. LLM 단독 판정 금지. 검사기 초록 ≠ 배포 안전.

## 5. 이 저장소 구성
- `docs/CODEX-01~04-*.md` : 하네스 경량화·스킬자동화·번들링·수정설계 검토(이전).
- `docs/CODEX-05-검사기-한계-정직분석-및-LLM검증.md` : **정본**(능력·한계 + LLM 검증 설계).
- `docs/CODEX-05-전략발표.html` / `.pptx` / `_gen.py` : 발표덱(디자인/편집/생성기).
- `docs/HISTORY-2026-08-10.md` : 이 핸드오프.
